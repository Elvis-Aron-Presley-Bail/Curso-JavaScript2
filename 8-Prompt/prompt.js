let alunos = []

let numAlunos = parseInt(prompt("Quantos alunos voce quer cadastrar?"))

for(let i = 0; i < numAlunos; i++) {
    let nome = prompt(`Digite o nome do aluno ${i + 1}: `)
    let idade = parseInt(prompt(`Digite a idade do aluno ${i + 1}: `))
    let nota1 = parseInt(prompt(`Digite a nota ${i + 1}: `))
    let nota2 = parseInt(prompt(`Digite a nota ${i + 1}: `))
    let nota3 = parseInt(prompt(`Digite a nota ${i + 1}: `))
    let nota4 = parseInt(prompt(`Digite a nota ${i + 1}: `))
}
